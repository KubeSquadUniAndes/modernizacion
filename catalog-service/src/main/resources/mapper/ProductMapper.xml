<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.mybatis.jpetstore.catalog.mapper.ProductMapper">

  <select id="getProduct" resultType="Product">
    SELECT productid, name, descn AS description, category AS categoryId
    FROM product
    WHERE productid = #{productId}
  </select>

  <select id="getProductListByCategory" resultType="Product">
    SELECT productid, name, descn AS description, category AS categoryId
    FROM product
    WHERE category = #{categoryId}
  </select>

  <select id="searchProductList" resultType="Product">
    SELECT productid, name, descn AS description, category AS categoryId
    FROM product
    WHERE lower(name) LIKE #{keyword}
  </select>

</mapper>
